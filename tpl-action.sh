#!/bin/bash

# If using a virtual environment, enter full path to python binary and full path to manage.py

python3 /path/to/manage.py --ip {IP-ADDRESS} --id {DEVICE-ID} --key '{DEVICE-LOCAL_KEY}' $1
